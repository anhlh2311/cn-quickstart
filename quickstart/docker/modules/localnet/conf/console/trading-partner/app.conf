canton.remote-participants.trading-partner {
  ledger-api {
    address = canton
    port = 1${PARTICIPANT_LEDGER_API_PORT_SUFFIX}
  }
  admin-api {
    address = canton
    port = 1${PARTICIPANT_ADMIN_API_PORT_SUFFIX}
  }
}
