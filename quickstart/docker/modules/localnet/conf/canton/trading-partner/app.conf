canton.participants.trading-partner = ${_participant} {
  storage.config.properties.databaseName = participant-trading-partner
  monitoring {
    http-health-server.port = 1${CANTON_HTTP_HEALTHCHECK_PORT_SUFFIX}
    grpc-health-server.port= 1${CANTON_GRPC_HEALTHCHECK_PORT_SUFFIX}
  }
  http-ledger-api.port = 1${PARTICIPANT_JSON_API_PORT_SUFFIX}
  admin-api.port = 1${PARTICIPANT_ADMIN_API_PORT_SUFFIX}
  ledger-api.port = 1${PARTICIPANT_LEDGER_API_PORT_SUFFIX}
}
